import { ListItemProps } from '@gravis-os/ui'
import { routes } from './routes'

const navConfig: ListItemProps[] = [
  { key: 'home', title: 'Home', href: routes.POS_HOME },
]

export default navConfig
